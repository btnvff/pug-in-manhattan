# PROJECT.md — паспорт проекта (читать перед любой правкой)

Этот файл общий для всех, кто работает с кодом: человек, Claude, ChatGPT/Codex.
Если после правки что-то в этом файле устарело — обнови его в той же правке.

## Что это
«Мопс на Манхэттене» — мобильная аркада (портретная ориентация, управление пальцем).
Мопс внизу экрана, сверху падает еда. Ловишь вкусное, избегаешь овощей и кирпичей.
Это скетч-концепт. Добавлен первый 3D-прототип: прежняя механика, объёмные процедурные модели, фиксированная ортографическая камера. Canvas 2D сохранён как отдельный режим и запасной рендер. Планы: проверка PWA и 3D на iPhone, затем приложение в App Store.

## Как запустить
- Играть: https://btnvff.github.io/pug-in-manhattan/ (3D по умолчанию; `?view=2d` — исходный режим).
- Просто открыть `index.html`, либо `python3 -m http.server` в корне и открыть http://localhost:8000
- По умолчанию открывается 3D. Для исходной графики: `index.html?view=2d`. При недоступном WebGL игра автоматически использует Canvas 2D; потеря WebGL-контекста или ошибка 3D-анимации/рендера/resize ставит игру на паузу, освобождает ресурсы 3D и переключает в 2D без остановки RAF.
- Публикация: GitHub Pages из ветки `main`, корень репозитория. Все пути относительные (`./`) — не менять на абсолютные.

## Технические правила
- Только обычные скрипты: без npm, без сборщика, без ES-модулей в приложении. Всё в глобальной области видимости. Единственная библиотека — локальная Three.js r185 (MIT), `js/vendor/three-r185.js`: оригинальная CommonJS-сборка в оболочке обычного скрипта. CDN не используется.
- Оболочка Three.js использует отдельный генератор случайных чисел для UUID: создание моделей не должно расходовать `Math.random()` игровой логики. Не удалять эту изоляцию при обновлении библиотеки.
- **Порядок подключения скриптов в `index.html` важен** (`balance.js` первым, `render.js` последним; Three.js перед `pug-3d.js` и `pug-animation.js`, затем `models-3d.js`, `world-3d.js` и `render-3d.js`). Новый файл — добавлять в нужное место и упомянуть здесь.
- Логические размеры сцены: около 390×844 (`W`, `H` в `game.js`), реальное разрешение canvas масштабируется по `devicePixelRatio` (потолок в `BALANCE.frame.maxDpr`).
- Весь игровой баланс — только в `js/balance.js`. Числа не хардкодить в других файлах.
- Интерфейс и тексты — на русском.
- Настройки и рекорд хранятся в `localStorage` под ключом `manhattan-pug-progressive` — не переименовывать без миграции.
- Звук синтезируется через WebAudio (`audio.js`), файлов звука нет. На iOS звук стартует только после касания (`unlockAudio`).
- Проверять на мобильном размере (390×844, тач) и в портретной ориентации; горизонтальная есть, но вторична.

## Файлы
| Файл | Что внутри |
|---|---|
| `js/balance.js` | Вся настройка: очки, «счастье», сложность, шансы, длительности бонусов, ритм волн |
| `js/game.js` | Состояние (`state`: menu / play / …), главный цикл логики, спавн еды, ввод (pointer, клавиатура), меню, HUD |
| `js/run-state.js` | Состояние забега: самочувствие мопса, комбо, near-miss, обратная связь |
| `js/powers.js` | Бонусы (магнит, щит, x2, помощники-коты и птицы и др.) |
| `js/events.js` | События: дождь из еды, цепочки, волны, дебаффы (заморозка, ветер и т.д.) |
| `js/audio.js` | Звук и музыка (WebAudio) |
| `js/render.js` | Выбор рендера, автоматический переход в 2D, `requestAnimationFrame` |
| `js/render-2d.js` | Исходная Canvas-сцена |
| `js/render-feedback.js` | Общая экранная обратная связь и вынесенные Canvas-функции эффектов |
| `js/render-3d.js` | Three.js-рендер, привязка моделей к состоянию, пулы объектов, обработка потери WebGL |
| `js/pug-3d.js` | Отдельная 3D-модель героя, собственные материалы, мягкие тени, ресурсы и dispose |
| `js/pug-animation.js` | Детерминированные позы героя: idle/look/movement/catch/bad, моргание и редкое дыхание |
| `js/models-3d.js` | Фабрика 9 типов предметов, котов и птиц; делегирование героя; dispose общих ресурсов |
| `js/world-3d.js` | Объёмная улица, здания, мост, транспорт, прохожие и атмосфера |
| `js/vendor/three-r185.js`, `js/vendor/three-LICENSE.txt` | Локальная библиотека Three.js и MIT-лицензия |
| `tests/gameplay.test.js` | Проверки логики без браузера и сравнение с исходным Git-коммитом |
| `tests/browser.test.js` | Браузерные проверки через внешнюю установку Playwright; проверка выбранного режима перед сравнением забегов |
| `tests/character.test.js` | Геометрия, чистота позы, опора лап, края экрана, владение ресурсами |
| `tests/character-browser.test.js` | Девять состояний героя, точная пауза и GPU-проверка относительно независимой базы рендерера |
| `tests/render-lifecycle.test.js` | Ошибки создания/анимации/рендера/resize, потеря контекста и повторный dispose |
| `tests/browser-offline.js` | Загрузка локального содержимого для DOM/WebGL-тестов без HTTP-навигации |
| `CHARACTER_REVIEW.md` | Направление персонажа, результаты проверки и ограничения character update |
| `js/drawing.js` | Мелкие помощники рисования (ellipse, path, rect…) |
| `js/characters.js` | Рисование мопса, людей, машин, котов |
| `js/food.js` | Рисование еды |
| `js/world.js`, `js/world-details.js` | Рисование города, фона, атмосферы |
| `style.css` | Интерфейс поверх canvas (меню, HUD, панели) |

Типы еды (индекс в `BALANCE.food`): 0 сосиска, 1 печенье, 2 куриная ножка, 3 пончик, 4 брокколи, 5 морковь, 6 цветная капуста, 7 косточка (бонус), 8 кирпич. Вредные: 4–6 и 8 (`isHazard`).
Каждый предмет в массиве `items`: координаты `x`, `y` в логических пикселях, `type`, плюс служебные поля.

## Граница логики и графики
`game.js`, `run-state.js`, `powers.js` и `events.js` сохраняют общую логику и состояние. Функции Canvas-отрисовки эффектов перенесены в `render-feedback.js`. Рендеры только читают игровое состояние; создавать новые случайные игровые события или менять баланс в них нельзя.

Оба рендера используют те же логические пиксели. Ортографическая камера 3D переводит `(x, y)` в `(x - W/2, H/2 - y)`, поэтому спавн, ввод и зоны ловли остаются прежними. WebGL-canvas находится под исходным canvas: верхний слой принимает ввод и рисует подписи/индикаторы. DOM-интерфейс, звук и localStorage общие.

3D-модели созданы программно; это первая художественная версия, не автоматическая конвертация Canvas-графики. Геометрия и материалы переиспользуются, статические блоки города объединены в `InstancedMesh`. Игровое состояние пока остаётся глобальным — это не самостоятельный движок или переход к свободному перемещению по улице.

## Персонаж: границы обновления
Модель героя выделена из общей фабрики. Его тёмная маска окрашена в вершинах самой головы; глаза уменьшены и уплощены, уши имеют форму мягких складных лоскутов. Опора лап компенсирует наклон, squash/stretch корпуса и сгиб голеностопа. Тень и редкий пар используют одну небольшую DataTexture. Поза читается из уже существующих игровых сигналов: анимация не меняет RNG, зоны ловли, управление и баланс; пауза сохраняет точные трансформации.

Ресурсы разделены между `hero.userData.dispose()`, `model.dispose()` и `world.dispose()`. Завершение 3D-view вызывает их один раз, снимает обработчик потери контекста до его принудительного освобождения и удаляет canvas. Runtime-сбой переводит игру через `fallbackToCanvas()` в 2D и не обрывает RAF.

## Проверки
- `node tests/gameplay.test.js` — очки, бонусы, пауза, перезапуск и воспроизводимые забеги.
- `node tests/gameplay.test.js --compare 5217db3` — сравнение с исходной 2D-логикой: три забега по 120 секунд.
- `node tests/browser.test.js` — если Playwright и Chromium установлены отдельно. Можно задать `PLAYWRIGHT_MODULE` и `CHROMIUM_PATH`; `SCREENSHOT_DIR` включает сохранение скриншотов. Сам тест поднимает локальный HTTP-сервер.
- `node tests/character.test.js` — геометрия и 3600 кадров поз, 240 крайних поз, чистота сигналов, пауза, корректный dispose.
- `node tests/character-browser.test.js` — реальные DOM/WebGL, девять состояний, три размера экрана, шесть циклов GPU-ресурсов. Требует внешние Playwright/Chromium.
- `node tests/render-lifecycle.test.js` — шесть сценариев сбоев/очистки, сохранность забега, продолжение в Canvas и RAF.
- В среде без HTTP-навигации `OFFLINE_BROWSER=1 node tests/browser.test.js` загружает локальное содержимое. Два новых браузерных теста используют этот способ напрямую. Это не проверка HTTP, GitHub Pages или Safari. На безэкранном Linux может потребоваться `xvfb-run -a`.
- GPU-тест прогревает служебную DFG LUT из Three.js отдельным материалом до создания героя; освобождение проверяется относительно этой независимой базы, а не ложного требования нуля всех текстур рендерера.
- Браузерная проверка покрывает 390×844, 320×568, 844×390, тач-перетаскивание, паузу, эффекты, совпадение забегов 2D/3D и запасной рендер.
- Реальный Safari/iPhone, производительность на устройстве и качество звука требуют отдельного испытания. Service worker/офлайн-кэш пока отсутствует.

## Дорожная карта
1. [ ] Публикация на GitHub Pages, PWA (manifest + иконки), тест на iPhone по ссылке
2. [x] Вынести Canvas-отрисовку из логики; сохранить поведение и общий игровой цикл
3. [x] Первый 3D-прототип на Three.js в рабочей ветке `chatgpt/3d-evolution` (та же логика, новый рендер); 2D-версия остаётся рабочей. Публикуется через GitHub Pages из `main`
4. [ ] Обёртка Capacitor и приложение для iOS (нужны Mac/Xcode и платная программа разработчика Apple)

## Правила совместной работы (человек + несколько ИИ)
- `main` всегда рабочая: игра открывается и играется. Прямых больших правок в `main` нет.
- У каждого исполнителя своя ветка: `claude/<тема>`, `chatgpt/<тема>`. Слияние через pull request.
- Один файл не правят двое одновременно. Перед большой задачей — написать в PR/чате, какие файлы затронешь.
- Правки — маленькие и точечные. Не переформатировать файлы целиком и не переименовывать без причины: это ломает слияние.
- Не менять баланс «по ходу» вместе с другой задачей: баланс — отдельным коммитом.
- После каждой значимой правки — одна строка в журнале ниже.

## Журнал изменений
- 2026-09-20: проект перенесён на GitHub, добавлены `manifest.webmanifest`, иконки, `PROJECT.md`. Игровой код не менялся.

- 2026-09-23: добавлен локальный 3D-прототип на Three.js, Canvas-отрисовка отделена от логики, сохранён режим 2D и добавлены проверки совпадения поведения. Баланс не менялся.

- 2026-09-23: 3D-версия подготовлена к публикации на GitHub Pages через `main`; добавлена ссылка на игру.
- 2026-09-23: обработчик изменения размера подключается после инициализации всех скриптов, чтобы раннее мобильное событие resize не прерывало загрузку; добавлена регрессионная проверка.

- 2026-09-23: подготовлен локальный character update; модель и детерминированные анимации отделены от общей фабрики. Исправлены остановка RAF при ошибке 3D и неполная очистка при ошибке создания; добавлены character/lifecycle-регрессии. Баланс и игровая логика не менялись.
